# Audit Artifacts

This directory stores point-in-time audit artifacts that were reviewed or annotated manually. Use dated filenames so each artifact keeps its historical context and can be traced back to the audit cycle that produced it.

These files may become seed sources for V0.6 classification tables, especially where manual review identifies fulfillment leak categories, group billing patterns, or suppression decisions that should be promoted into structured data.
